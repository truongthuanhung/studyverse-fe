export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other'
}

export enum Role {
  Student = 'student',
  Teacher = 'teacher'
}

export enum StudyGroupPrivacy {
  Public,
  Private
}

export enum StudyGroupRole {
  Admin,
  Member,
  Guest
}
